export default function SectionPill({ id, title }) {
  return (
    <div id={id} className="sub-section container-g">
      <div className="section-pill">
        <h2>{title}</h2>
      </div>
    </div>
  );
}
